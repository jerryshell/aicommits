// Label formatters
export const CURRENT_LABEL_FORMAT = (model: string) => `✅ ${model} (current)`;

export const MAX_DIFF_LENGTH = 30000;
export const DIFF_TRUNCATED_SUFFIX = "\n\n[Diff truncated due to size]";
